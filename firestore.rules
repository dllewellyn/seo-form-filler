rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      // For this localized SEO tool, allow all read/writes. 
      // In production with multiple users, these would be locked down.
      allow read, write: if true;
    }
  }
}
